import 'package:flutter/material.dart';

class Revisi extends StatelessWidget {
  const Revisi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
